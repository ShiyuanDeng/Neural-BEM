# Middle circle split to diagonal ellipse and star

Outcome: **full_pass**.

Profile: `full`. Proposal policy: `background_td_replacement`. Observation source: independent analytic truth curves discretized at 256 nodes/component. The inverse used only 0.50 GHz, 1.50 GHz; all other frequencies are holdouts. Truth geometry is used only for final qualification and the dashed video overlay.
