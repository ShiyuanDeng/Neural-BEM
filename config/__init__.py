"""Simulation configuration package for the Neural-SDF/IBIM pipeline.

This file is intentionally minimal. Its presence makes ``config`` a regular
package rather than a PEP 420 namespace package, so that the local directory
wins the ``sys.path`` scan even in environments that ship an unrelated
pip-installed distribution also named ``config``.
"""
