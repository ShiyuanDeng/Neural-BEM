# TOP-023 pre-dispatch owner review

Owner/reviewer: Codex `/root`. No independent review claimed.

The numerical API receives a frozen terminal state, training samples/predictions
and model configuration. Preparation strips all development columns and scores;
neither truth nor evaluation is passed to the audit. No inverse function is
called and no candidate replaces the base. The same base and model feed all arms.

The diagnostic reuses physical prediction, gauge/feasibility, normalized residual,
feasible-side quotient and derivative qualification helpers. It reproduces the
existing LM algebra exactly at four declared damping values. It does not change
shared numerical sources or implement a new production optimizer. Selected
two-scale/two-resolution checks qualify only their stated directions.

Whole-model (272), remaining directional/repeatability (52), and candidate (8)
reservations precede physical work. The existing ledger installs a hard 600-call /
1200-second limit and records every charged frequency attempt. All candidates
are scored at both resolutions and existing acceptance margins remain binding.
The first qualified decreasing candidate ends each arm; failures remain saved.

Input and source/test/log/history hashes bind preparation and sealing.
The inherited TOP-022 source guard must also pass before importing its terminal
inputs; a changed numerical implementation is not silently part of the diagnosis.
Raw complex probe predictions, aliases/stencils, the full model and candidate steps are saved.
The separate NumPy-only reporter replays normalization, model/gradient algebra,
selected directional checks, step algebra, acceptance, numerical gates, work and
the predeclared 2× gain/no-more-calls gate. No BIE import is in the reporter.

The result can release only a separately declared bounded continuation comparison.
It cannot certify recovery, select by truth, extend TOP-022 or release TOP-021.
