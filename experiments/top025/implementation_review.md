# TOP-025 implementation owner review

Owner: Codex `/root`; no independent-agent review claimed.

The current user explicitly requests all-case current-code measurements and
videos. This supersedes the previous decision not to spend a suite for this
descriptive evaluation. TOP-021's matched S/F release guard remains unchanged;
TOP-025 has one integrated F candidate, with no promotion/comparison claim.

Use original fresh starts and data, the inherited H topology loop, TOP-021's
returned-count capacity rule, TOP-017's stage schedule and ledger, and TOP-020's
endpoint scoring. The optimizer receives training observations only. No shared
solver, optimizer default, recovery gate or historical numerical artifact changes.

All twelve rows are retained. Four subprocesses each have independent ledgers,
signal timers and an external timeout. The campaign cap is the sum of phase
maxima, with no retries or extensions. Source/input integrity is checked before
the handoff and after numerical completion; every measured file is archived.

Tests cover scene coverage and original columns, fail-closed oracle preparation,
fresh starts and no supplied count, training-only fitting, exact quotas, budget
and numerical hard stops, partial rows and accepted-frame provenance. Inherited
tests cover the unchanged capacity, derivative, acceptance and ledger logic.

Reporting reuses independent saved-array verification, scores retained geometry
under a zero-call ledger, preserves unavailable final predictions, and marks
partial work as a lower bound. Video rendering imports no solver. It shows every
saved accepted record at least once, repeats frames without interpolating
coefficients, ends at the scored retained state, and records input/output hashes.
Every MP4 is probed; final overlays and representative frames receive visual QA.
