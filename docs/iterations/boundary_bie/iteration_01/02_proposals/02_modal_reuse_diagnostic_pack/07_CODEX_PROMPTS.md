# Copy-paste prompts

Choose the prompt matching the action you are authorizing. Extracting this archive alone is not numerical approval. These prompts do not authorize new branches/worktrees.

## Review and prepare only

```text
Read docs/iterations/boundary_bie/iteration_01/02_proposals/02_modal_reuse_diagnostic_pack/00_README.md and the linked contract, validation rules and repository handoff.

Review the packet against the current checkout and existing governance. Preserve all prior results and user changes. Resolve material recommendations once as accept/reject/defer/named diagnostic; do not begin an open-ended redesign. Prepare the narrowly scoped BIE-002 plan and update the Boundary–BIE handoff truthfully. BIE-002 remains PROPOSED — NOT APPROVED FOR EXECUTION unless an explicit named approval already exists. No numerical code changes or runs in review-only mode.

Idea 2 is a fixed-geometry modal/structure diagnostic, not a production solver replacement. Idea 3 is deferred. Neither targets FD gradients. Existing analytic sensitivities are intended; FD is optional verification only. Do not start a new iteration or invent results. Tell me the single approval/action required next.
```

## Approve and execute the bounded primary diagnostic

Issue this prompt only when you intend to approve the specified numerical work:

```text
Approve BIE-002 only, at the scope and hard budgets in docs/iterations/boundary_bie/iteration_01/02_proposals/02_modal_reuse_diagnostic_pack/. This named approval does not approve idea 3, BIE-004, an inverse campaign, production integration, or new branches/worktrees.

Read 00_README.md, 02_BIE002_MODAL_DIAGNOSTIC.md, 04_VALIDATION_COST_AND_STOP_RULES.md and 05_REPOSITORY_HANDOFF.md, plus the current repository instructions. Work in the existing /home/drdeng/Neural_SDF_BEM_AD checkout on feature/ordered-boundary-nystrom. Preserve dirty work and do not overlap another numerical-code writer.

First reconcile the live interfaces and perform a bounded contract review. Consolidate the agreed BIE-002 plan and truthful approval/execution status. Then implement only the experiment-local diagnostic and run it within the global counters and execution cap. Do not modify shared numerical solver code. If a material change of scope is needed, stop that part with a precise blocker rather than expanding the project.

Compare modal reduction with both a refined reference and the cheapest accuracy-qualified reduced-node Kress control. Test full-mode equivalence, nontrivial operator structure, and analytic directional sensitivity accuracy where already supported. Differentiate the actual reduced equations. No FD-based inverse or full FD Jacobian; FD is optional validation only. Missing multi-component analytic derivatives stay explicitly unqualified, not silently implemented.

Count full assembly, projection, factorization, derivative work, validation and memory. A basis change or a dense projected matrix is not a quadrature-free speedup. Preserve failed and incomplete arms. Stop when a budget/gate is reached; do not rescue a failed hypothesis by changing tolerances or inventing a new method.

Close with source changes, tests actually run, fresh artifact paths, separate H2-FIELD/H2-OPERATOR verdicts, and one recommended next decision. Do not execute a successor. No automatic commit or push.
```
