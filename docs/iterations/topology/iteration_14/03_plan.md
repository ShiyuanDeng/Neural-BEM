# TOP-022 — approved fresh direct four-frequency recovery

- **Approval status:** APPROVED
- **Execution status:** COMPLETE
- **Authority:** 2026-09-15 user “cp first. then i approve you to finish the rest”.
  TOP-019 was committed/pushed first as `fa2666f`. This is the next bounded
  diagnosis after TOP-020's failed fresh recovery, within that remaining-work
  instruction; no further branch/worktree is authorized.
- **Owner:** Codex `/root`; **independent reviewer:** unassigned, owner review.
- **Review and file/API map:** [resolved contract](02_proposals/01_direct_four_frequency_contract_and_review.md).

## Contract

Repeat H freshly from the frozen original distant circle on 0.5 GHz, at 64/128
nodes, maximum 4,000 charged calls / 600 seconds. Preserve the H configuration,
material, radius floor, event validity and monotonicity. Only `recovered` or
`topology_stationary` may hand off. Verify its computed endpoint equals the
TOP-020 prefix hash; never initialize fitting from an archived optimized state
or a supplied object count.

Zero-pad the returned components to K9 at identical geometry/IDs. Require
256/512 feasibility and numerical qualification. Immediately fit the full
0.5/0.75/1/1.25-GHz training objective. One prescribed stage 4, quota **8,000**
including twelve endpoint scoring calls, plus twelve initial scoring calls:
**8,012 total / 7,200 seconds**. Maximum 22 optimizer updates, FD step 1e-4,
gradient/relative-step tolerances 1e-7, objective target 1e-14, unchanged step
limits, damping, gauge, feasible FD and loss acceptance; loss-change stopping
disabled. Reserve complete models/steps and final scoring. Keep all hard stops.

Total ceiling **12,012 charged calls / 7,800 active seconds**, exactly TOP-020's
ceilings, one physical worker and one BLAS thread. This reallocates the same
budget to one full-frequency objective. No new observations, oracle solve,
frequency, threshold, count restriction, restart or best-state selection.

Reuse the exact original 24-pair training and development-evaluation records.
Numerical tolerances remain 1e-7 at 0.75/1/1.25 GHz and 1e-5 at 0.5/1.5/2.5 GHz.
The sole final endpoint is the end of this stage 4, even if a quota stops it.
Require effective model exposure, numerical qualification and the original
count/boundary <=1 mm/IoU >=0.90/training <=0.003/evaluation <=0.05 gates.
Truth and evaluation data only score prescribed endpoints.

## Artifacts and decision

Create `results/validation/topology/TOP-022-<timestamp>-fresh-direct-two-stars/`:
inputs, contract, source-bound tests and snapshots, old failed comparison hashes,
fresh topology/events/work, handoff identity, full-frequency trajectories and
terminal diagnostics, both-resolution complex predictions, independently
replayed scores and work, figure and owner closeout. Results open iteration 15.
The completed TOP-020 negative bundle and TOP-018/019 positives remain intact.

Pass supports a separately declared twelve-scene direct-protocol comparison.
Failure keeps that suite gated and triggers a bounded stationarity/derivative/
entry-basin diagnosis using this new evidence. Do not claim that this single
protocol comparison proves a unique failure cause or a generally qualified
controller. TOP-021's staged suite remains NOT STARTED.

## Dispatch

112 pre-dispatch tests pass (32.57 seconds; zero physical solves). Source-bound
validation freezes 199 source files and seven test files.
Output: `results/validation/topology/TOP-022-20260915-165552-fresh-direct-two-stars`. One numerical worker, BLAS 1.

### In-progress checkpoint (not a recovery result)

Fresh H finished at `topology_stationary`, 1,615 charged calls in
131.808 seconds, and exactly reproduced TOP-020’s endpoint.
The zero-padded handoff and initial predictions qualify. Full-frequency update
4 is accepted at objective 0.000166804925. The final result is pending;
no source in the measured manifest has changed.

## Closeout

The run completed its single stage with 22 accepted updates and stopped at
`maximum_iterations`. Numerical gates pass; boundary/IoU/development recovery
fail. Total 8,247 charged calls, 8,180 completed systems, 67 charged geometry
refusals, 3,205.137761 active seconds. All 199 measured source files and frozen
inputs remain unchanged. 112 pre-dispatch tests and saved-artifact replay pass;
the final figure was visually checked. No budget extension or extra restart.
The measured terminal reduced gradient is 0.0142759872, above its 1e-7 test.
Results open [iteration 15](../iteration_15/01_results.md); a bounded terminal
training-model diagnosis precedes any further inversion. TOP-021 is not released.
