# TOP-020 — fresh automatic two-star integration

## Authority and decision

On 2026-09-15 the user confirmed the next fresh automatic two-star experiment
with “yeah go”, then instructed: **“cp first. then i approve you to finish the
rest”**. TOP-019 was validated, committed and pushed as `fa2666f` first, with a
clean tree and verified remote. This later instruction authorizes the remaining
topology roadmap, including bounded successor implementation and conditional
qualification, without another per-ID permission request. It does not authorize
another branch/worktree. The existing branch remains the implementation checkout.

Record each successor's scope, budget and evidence before dispatch. Preserve
all failed attempts. A failed focused gate must stop the proposed suite and
lead to a bounded diagnosis; permission to finish does not turn a failure into
qualification or remove resource limits. No independent agent is assigned.

## Question

Does the existing H automatic controller, followed by qualified K9 cumulative
continuation at 256/512 nodes, recover `far-two-stars` from its frozen original
distant circle without a supplied count or saved optimized initialization?

## Source review and resolutions

- **Accept** the existing `run_topology_recovery_followup.py::topology_prefix`
  and `TopologyLedger`: they preserve normal geometry refusals and charge TD
  systems alongside forward attempts. The prefix accepts training data only.
- **Accept** existing `run_top017.py::run_schedule` / `fit_stage`, with all four
  explicitly supplied stage quotas. Preserve loss-change stopping disabled,
  complete-model reservation, effective exposure and numerical hard stops.
- **Accept with amendment** the central handoff pattern: zero-pad the fresh
  returned state to K9 and check feasibility at this experiment's 256/512 nodes.
  The historical helper's fixed 128/256 check is not this contract's check.
- **Accept** TOP-018's qualified resolution, unchanged training observations,
  tolerances, gauge and optimizer settings. Start with fresh stage 1; the saved
  COMMON state and any fitted/truth states are excluded from initialization.
- **Accept** saved complex predictions at the automatic endpoint and every
  continuation endpoint, plus event-margin and monotonicity replay. Final count
  and truth geometry are evaluation only and do not release continuation.
- **Defer** the twelve-scene comparison until TOP-020 passes. Preserve and
  hash-check the existing fresh central success; the later suite must include
  central again as a measured regression control.
- **Reject** re-running TOP-019 or changing global numerical defaults.

Owner review: Codex `/root`; no independent review claimed.

## File/API map and validation

Add `experiments/top020/run.py` for input freezing, the two existing phases,
artifact scoring/verification and a fresh-output command. Add focused tests in
`pytest/sdf_inverse/test_top020.py`. No optimizer, physical solver, topology
candidate or historical default changes are planned.

Tests must cover original-start/input provenance, count-independent padding,
256/512 qualification, stage progression and hard stops, topology event checks,
prediction replay, numerical/recovery separation, budgets and source integrity.
Run inherited follow-up and staged-optimizer tests with mocked physical calls
before dispatch. Store the source-bound test record and measured new sources.

The consolidated [approved plan](../03_plan.md) owns exact execution bounds.
