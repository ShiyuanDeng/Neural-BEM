# Boundary–BIE: bounded diagnostic handoff

Prepared 15 September 2026. Instructions only; no implementation or experiment has been performed by preparing this archive.

Extract this ZIP at the root of the existing Neural-BEM checkout, **without overwriting existing files**. It adds one proposal packet under the existing Boundary–BIE iteration 01. It does not open a new iteration or replace an agreed plan.

Start with [the packet README](docs/iterations/boundary_bie/iteration_01/02_proposals/02_modal_reuse_diagnostic_pack/00_README.md), then use [the Codex prompts](docs/iterations/boundary_bie/iteration_01/02_proposals/02_modal_reuse_diagnostic_pack/07_CODEX_PROMPTS.md).

**Decision:** investigate modal field/operator structure through a small diagnostic; defer substantial geometry-reuse development. Neither mechanism is designed around finite-difference gradients. Analytic derivatives are the intended sensitivity path; FD is optional validation only.

The packet is **PROPOSED — NOT APPROVED FOR EXECUTION**. The execution prompt contains explicit approval of BIE-002 for the user to issue when ready. Merely downloading, extracting, reviewing, or receiving this ZIP does not authorize numerical work. This follows the repository's existing named-ID approval rule.

Do not create a new branch/worktree, run inverse campaigns, alter the topology controller, or replace production Kress. The existing checkout/branch rule remains in force.

This packet narrows and supersedes the earlier conversational `fourier_operator_experiments.md` handoff for this study. Preserve that file as history; do not execute its broader two-track plan in parallel.
